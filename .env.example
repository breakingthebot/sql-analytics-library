# .env.example
# Template for environment variables. Duplicate this file as '.env' and fill in details.
# DO NOT commit the actual '.env' file.

# Override SQLite database path (optional, default: data/ecommerce.db)
SQL_ANALYTICS_DB_PATH=
